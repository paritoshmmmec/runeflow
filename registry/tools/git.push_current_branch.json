{
  "name": "git.push_current_branch",
  "description": "Push current branch to its configured upstream remote.",
  "tags": ["builtin", "git"],
  "outputSchema": {
    "type": "object",
    "properties": {
      "branch": { "type": "string" },
      "remote": { "type": "string" }
    },
    "required": ["branch", "remote"],
    "additionalProperties": false
  }
}
