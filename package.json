{
  "name": "runeflow",
  "version": "0.1.0",
  "description": "Hybrid markdown + DSL runtime for executable AI skills, typed steps, and JSON run artifacts",
  "type": "module",
  "bin": {
    "skill": "./bin/skill.js"
  },
  "scripts": {
    "test": "node --test",
    "validate:example": "node ./bin/skill.js validate ./examples/open-pr.skill.md"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
